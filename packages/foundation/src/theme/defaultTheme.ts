import { ThemeProps } from '../../../core/src/types';

const defaultTheme: ThemeProps = {
    colors: {
        base: 'rgb(25,25,25)',
    },
    typography: {
        fontFamily: {
            base: 'sans-serif',
        },
        lineHeight: {
            base: 1.345,
        },
    },
    spacing: {
        base: '1rem',
    },
    components: {},
};

export default defaultTheme;
