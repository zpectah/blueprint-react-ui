import { ThemeProps } from '../types';

export const mixin__FieldLabel = (theme: ThemeProps) => {
    return `
            margin: 0;
            padding: 0;      
        `;
};

export const mixin__FieldsetGroup = (theme: ThemeProps) => {
    return `
            margin: 0;
            padding: 0;
            border: 0;    
        `;
};

export const mixin__FieldsetGroupLegend = (theme: ThemeProps) => {
    return `
            margin: 0;
            padding: 0;      
        `;
};
